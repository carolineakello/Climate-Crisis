# Flood Early Warning — ML (Humanised)

**What it does:** Trains a RandomForest to predict flood risk from simple features.
Comes with a clean synthetic dataset generator.

## Run
```bash
pip install -r requirements.txt
python train.py
```
